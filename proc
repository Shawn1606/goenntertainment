# proc – Änderungs-Log für künftige AI-Sessions

Dieses File ist das laufende Protokoll aller Änderungen am Projekt Gönntertainment.
Jede AI-Session hängt **unten** einen Eintrag pro Ticket/Änderung an. Neueste zuletzt.

Kontext für neue Sessions: siehe `CLAUDE.md` (Index), `PROJECT_RULES.md` (Regeln),
`TASK_SYSTEM.md` (wie Tickets laufen). Aufgaben = GitHub-Board `Projects/2` (via `gh`).

Eintrags-Format:
```
## <YYYY-MM-DD> · <Ticket #N: Titel | chore>
- Geändert: <Dateien>
- Getan: <kurz>
- Getestet: <Pint / Tests / Browser>
```

---

## 2026-07-24 · chore: Workflow-Setup eingerichtet
- Geändert: `CLAUDE.md`, `PROJECT_RULES.md`, `TASK_SYSTEM.md`, `.githooks/` (commit-msg,
  pre-push, pint-baseline.txt=4), `.claude/commands/` (8 Skills), `tasklists/briefs/`, `proc`.
- Getan: Portablen Workflow (WORKFLOW-PORTABEL.md) an Laravel + GitHub-Board angepasst.
  Task-Quelle = Board Projects/2 (via gh). Hooks aktiviert (`git config core.hooksPath .githooks`).
  Check-Befehle: `php vendor/bin/pint --test` (Baseline 4) + `php artisan test` (5 grün).
- Getestet: commit-msg-Hook (blockt `@`-Subjects, akzeptiert Konvention). PHP über Herd
  `php.bat` (git-bash sieht kein `php`).
- Hinweis: `php artisan test` = 5 grün, alle Migrationen gelaufen (Stand dieser Session).

## 2026-07-24 · Ticket #1: Registration/Log-In – Views
- Geändert: `resources/views/welcome.blade.php` (neu aufgebaut), `resources/views/auth/`
  (register neu, login/forgot-password/reset-password/complete-google-registration),
  `resources/views/home.blade.php` (neu), `app/Http/Controllers/Auth/LoginController.php`
  (ungenutzte create() + View-Import entfernt), `routes/web.php` (GET login → Startseite).
- Getan: Startseite mit Login-Sheet, das per Klick/Hochwischen von unten hochfährt
  (Vanilla-JS, kein Alpine); Auto-Öffnen bei Fehlern/Status. Register im Design mit
  Interessen-Chips (min 3) + Konto-Typ. Alle Auth-Views nutzen `layouts.auth` + CSS-
  Bausteine (`auth-input`, `auth-btn-primary`, `interest-chip`, `account-type-option`).
- Getestet: Pint = Baseline (4, unverändert), Pest 5/5 grün. HTTP: `/`, `/register`,
  `/forgot-password` → 200 (welcome enthält Brand/Sheet/Login-Form). In-App-Browser
  konnte die lokale `.test`-Domain nicht klickend prüfen (Freigabe-Policy).
- Nachtrag: `InterestSeeder` (10 Einträge, existierte schon) auf Dev-DB ausgeführt –
  Register hatte vorher keine wählbaren Interessen ("interests required"). Zusätzlich
  deutsche Validierungs-Meldungen für `username.alpha_dash` und `password.letters/numbers`
  in RegisterRequest + CompleteGoogleRegistrationRequest ergänzt.
- Fix home.blade.php: `@{{ $user->username }}` gab wegen des `@` den Blade-Ausdruck als
  Text aus → geändert zu `{{ '@'.$user->username }}`, zeigt jetzt den echten Username.
