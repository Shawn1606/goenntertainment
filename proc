# proc – Änderungs-Log für künftige AI-Sessions

Dieses File ist das laufende Protokoll aller Änderungen am Projekt Gönntertainment.
Jede AI-Session hängt **unten** einen Eintrag pro Ticket/Änderung an. Neueste zuletzt.

Kontext für neue Sessions: siehe `CLAUDE.md` (Index), `PROJECT_RULES.md` (Regeln),
`TASK_SYSTEM.md` (wie Tickets laufen). Aufgaben = GitHub-Board `Projects/2` (via `gh`).

Eintrags-Format:
```
## <YYYY-MM-DD> · <Ticket #N: Titel | chore>
- Geändert: <Dateien>
- Getan: <kurz>
- Getestet: <Pint / Tests / Browser>
```

---

## 2026-07-24 · chore: Workflow-Setup eingerichtet
- Geändert: `CLAUDE.md`, `PROJECT_RULES.md`, `TASK_SYSTEM.md`, `.githooks/` (commit-msg,
  pre-push, pint-baseline.txt=4), `.claude/commands/` (8 Skills), `tasklists/briefs/`, `proc`.
- Getan: Portablen Workflow (WORKFLOW-PORTABEL.md) an Laravel + GitHub-Board angepasst.
  Task-Quelle = Board Projects/2 (via gh). Hooks aktiviert (`git config core.hooksPath .githooks`).
  Check-Befehle: `php vendor/bin/pint --test` (Baseline 4) + `php artisan test` (5 grün).
- Getestet: commit-msg-Hook (blockt `@`-Subjects, akzeptiert Konvention). PHP über Herd
  `php.bat` (git-bash sieht kein `php`).
- Hinweis: `php artisan test` = 5 grün, alle Migrationen gelaufen (Stand dieser Session).

## 2026-07-24 · feat(api): JSON-Auth-API für Expo-App (Sanctum + Google)
- Geändert: `app/Models/User.php` (HasApiTokens), `bootstrap/app.php` (api-Routing),
  `routes/api.php`, `config/cors.php`, `config/sanctum.php`, `.env.example` (FRONTEND_URL),
  neue Migration `personal_access_tokens`, `app/Http/Controllers/Api/`
  (AuthController, GoogleAuthController), `app/Http/Requests/Api/` (Register/Login/GoogleLogin),
  `tests/Feature/Api/AuthTest.php`, `.githooks/pint-baseline.txt` (4→3).
- Getan: Sanctum via `install:api`. Token-Auth-Endpunkte register/login/logout/user +
  Google-Login (`/api/auth/google`: App schickt Google-access_token → App-Token via
  Socialite `userFromToken`). Web-Blade-Views unangetastet. Trennung: App-Code lebt im
  separaten Repo `goenntertainment-app`, dieses Repo bleibt reines Backend.
- Getestet: Pint grün (Baseline 3), Pest 14/14 grün (9 neu). Echt per HTTP:
  `/api/user`→401, `/api/login` falsch→422 (sauberes JSON).
- PR: https://github.com/Shawn1606/goenntertainment/pull/22 (nach OK des Users gemergt).
- Offen beim User: eigener Google-OAuth-Client für Expo in Google Cloud Console.

## 2026-07-24 · Ticket #3 Schritt 1/2: Activity-Backend (feature/ticket-3-create-activity)
- Neu: Tabellen `activities` (user_id=Host, title, description, location, starts_at,
  banner_path nullable) + Pivot `activity_interest`. Model `Activity` (host(), interests()).
- API: `GET /api/interests` (public, InterestController), `GET|POST /api/activities`
  (auth:sanctum, ActivityController). Banner-Upload auf public-Disk (`storage:link`).
  Validierung `StoreActivityRequest`: title/description/location/starts_at required,
  banner nullable image max 5MB, interests max 5 (exists).
- Getestet: Pint sauber (Baseline unverändert 3), Pest 23/23 (10 neu). HTTP: interests 200,
  create 201, list 200. Test-Datensatz danach wieder gelöscht.
- PR: https://github.com/Shawn1606/goenntertainment/pull/24. Board #3 → In progress.
- Offen (Schritt 2, App-Repo): Plus-Button-Formular (Foto Galerie/Kamera, Interessen,
  Beschreibung, Ort, Datum/Zeit) an API anschließen; Home auf echte Activities umstellen.
